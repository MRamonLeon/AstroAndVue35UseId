---
import MyComponent from "../vue-components/MyComponent.vue"

interface Props {
	title: string;
}

const { title } = Astro.props;
---
This is a card with text: {title}
<MyComponent text="Text from the card"/>